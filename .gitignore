# Node
node_modules/
npm-debug.log

# ios/.gitignore (door Capacitor zelf aangemaakt) regelt al Pods/, de
# gekopieerde App/App/public/, DerivedData/ en xcuserdata/ — niet dupliceren.

# macOS
.DS_Store

# Gegenereerd door scripts/bouw-web.mjs uit ../mijnportaal + web-overlay/.
# Nooit committen: het is een kopie, en dan zouden twee repo's dezelfde
# bestanden bijhouden en uit elkaar lopen.
www/
